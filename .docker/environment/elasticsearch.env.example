# Elasticsearch Security remains enabled locally; HTTP is used only on the loopback-published port.
ELASTIC_PASSWORD=CHANGE_ME_generate_a_unique_elastic_bootstrap_password
ELASTICSEARCH_APP_USERNAME=nineforum_app
ELASTICSEARCH_APP_PASSWORD=CHANGE_ME_generate_a_unique_elasticsearch_app_password
ELASTICSEARCH_APP_ROLE=nineforum_app
