# Root credentials are only for local MinIO bootstrap. Use a scoped service account in production.
MINIO_ROOT_USER=nineforum-local-root
MINIO_ROOT_PASSWORD=CHANGE_ME_generate_a_unique_minio_root_password
