# 最终安全审计报告复核

复核日期：2026-08-20（Asia/Taipei）  
复核范围：`prd.md`、`design.md`、`implement.md`、`research/` 证据、`security-audit-report.md` 与当前工作树边界

## 结论

最终报告与 PRD、设计及研究证据一致，R1–R6 均有状态、证据与限制。风险总数为 13 项，分布为 1 项严重、6 项高危、4 项中危、2 项低危；13 个总览表项均有对应详情。未发现会阻止交付的文档问题。

## 已修正问题

1. `prd.md` 的七项验收标准仍为未勾选状态，但 `audit-matrix.md`、执行记录和最终报告已提供满足证据；已按事实全部勾选。
2. SEC-07～SEC-10、SEC-12、SEC-13 原先采用过度压缩的段落，未逐项显式列出 CWE/类别、攻击前提、数据流、置信度与验证建议；已补齐为结构化字段。
3. SEC-02～SEC-06 的部分证据只写文件简称或缺少 Compose 行号；已改为仓库相对路径与精确行号。
4. SEC-11 原先缺少独立的攻击前提/数据流复核与影响字段；已补齐，并保留“扫描器原始级别不等于项目级严重度”的限定。

## 一致性验证

- 覆盖：`audit-matrix.md` 明确列出 R1–R6，均标记完成，并分别给出证据与限制。
- 风险统计：总览表与详情标题均为 13 项；严重/高危/中危/低危分别为 1/6/4/2。
- 扫描口径：OSV 为已完成的 146 个去重 `compile`/`runtime` Maven 坐标批量查询，返回 80 条原始公告（5 Critical、28 High、35 Moderate、12 Low）；OWASP Dependency-Check 12.2.2 因无 NVD API Key，在首次同步约 40,000/380,852（约 11%）时主动停止并以退出码 1 结束，报告未将其表述为完成。
- 供应链来源：复核时官方公告仍支持 Spring Security 6.5.3、Spring Framework 6.2.10、Spring Boot 3.5.5 和 MinIO Java 8.5.17 的受影响/修复版本表述；Spring Boot 官方发布说明确认 3.5.16 为 3.5.x 最后一个 OSS 版本，并建议尽快迁移到 4.0.x 或 4.1.x。报告的升级顺序合理，且没有把条件不成立的高分线索计为已确认漏洞。
- 动态边界：报告如实记录启动期 `@PostConstruct` 数据库刷新副作用、停止动作、无净业务数据变化、最终通过断开数据库连接完成匿名 GET 验证，以及未发送真实 STOMP 广播或业务写请求。
- 备份：文件大小 111130 字节，SHA-256 为 `8561562E3514C213B0AD5674874AFDE76FD7E9407633F379152D90B51AA69072`，包含 44 个 `CREATE TABLE` 和 41 个 `INSERT INTO` 段；文件路径被 `.gitignore:37` 的 `/.trellis/tasks/*/backups/` 命中。
- 敏感信息：任务 Markdown 未匹配配置中的秘密字面量、JWT 形态字符串、私钥头或邮箱地址；报告仅记录秘密类型、位置和指纹出现次数，不回显真实值或个人数据。
- 工作树：`git status --short` 仅显示根 `.gitignore` 被修改、当前任务目录为未跟踪；`git diff --name-only HEAD` 仅显示 `.gitignore`。未修改产品源码。

## 保留限制

- 专用 SAST、秘密、容器镜像与 DAST 扫描器未安装；该限制已写入报告，人工检查未被冒充为专用扫描器结果。
- Dependency-Check 的 NVD 同步未完成，不能据此排除更多已知漏洞；OSV 也不提供调用级可达性证明或容器镜像覆盖。
- 需要双账号或真实普通用户凭据的 Sticker/STOMP 场景未做动态利用，结论来自源码调用链二次复核；这是审计非破坏性边界，不是遗漏表述。
- 根 Maven 打包失败源于现有 Spring Boot 插件配置；本任务只审计不整改，未修改产品 POM。

## 质量门

- Lint：不适用；仓库未定义 Markdown linter，已人工检查标题、表格、链接与占位文本。
- TypeCheck：不适用；本次没有代码或类型变更。
- Tests：已有根目录 `clean test` 证据为 514 个测试全部通过；终检未重跑测试，因为本轮只修改审计文档。
- 产品代码：未修改。

